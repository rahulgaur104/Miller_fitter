# Miller_fitter
A simple Python code to fit Miller parameters to a flux surface.

This code is not optimized for speed hence it will be slow. 

Requires a non-standard python library called lmfit that can be installed using anaconda:

conda install -c conda-forge lmfit
